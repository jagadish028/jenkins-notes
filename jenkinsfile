pipeline {
      agent any
      stages {
          stage{'Build'} {
              steps {
                  sh '''
                  sleep5
                  echo "this is build"
                  '''
              }
          }
      }
}
